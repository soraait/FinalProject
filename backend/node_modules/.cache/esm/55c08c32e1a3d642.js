let dotenv,express,server,mongoose;_e1a‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_e1a‍.w("express",[["default",["express"],function(v){express=v}]]);_e1a‍.w("./server",[["default",["server"],function(v){server=v}]]);_e1a‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);
dotenv.config()






const {DB_USER , DB_PASSWORD , DB_NAME , PORT} = process.env
// const DB_USER="Soraait"
// const DB_PASSWORD="M0pxUXTT29mjbEzv"
// const DB_NAME="FinalProject"
// const PORT=4444

  const createServer = async () => {
    try {
      
      await  mongoose.connect(`
          mongodb+srv://${DB_USER}:${DB_PASSWORD}@graphql-basic.eduoc.mongodb.net/${DB_NAME}?retryWrites=true&w=majority`,
          { useUnifiedTopology: true }
      )

  const app = express()

  server.applyMiddleware({ app });

  app.listen({ port: PORT }, () =>
  console.log(`🚀 Server ready at http://localhost:${PORT}${server.graphqlPath}`)
  )
      
    } catch (error) {
      _e1a‍.g.console.log(error)
    }
  }

  
createServer()