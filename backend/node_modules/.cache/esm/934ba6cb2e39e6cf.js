let mongoose;_2e3‍.x([["default",()=>_2e3‍.o]]);_2e3‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const userSchema = new mongoose.Schema({
    name : {
        type: String,
        require: true
    },
    email : {
        type: String,
        require: true,
        lowercase: true,
        trim: true
    },
    password : {
        type: String,
        require: true,
        trim: true
    },
    products: [
        {
            type: mongoose.Schema.Types.ObjectId,
            ref: 'Product'
        }
    ],
    createdAt : {
        type : Date,
        require : true,
        default : () => Date.now()
    }
})

const User = mongoose.model('User', userSchema)

_2e3‍.d(User);