let jwt;_ad4‍.x([["default",()=>_ad4‍.o]]);_ad4‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const getUser = token => {
    
    if (!token) return null

    // 'Bearer zbowltobla..' ->[Bearer,zboddo]
    const parsedToken = token.split(' ')[1]
    try {
    const decodedToken = jwt.verify(parsedToken, process.env.SECRET)
        
    return decodedToken.userId
    } catch (error) {
        return null
    }
}
_ad4‍.d(getUser);